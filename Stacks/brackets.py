# Massad Raza
# Using the data structures stacks to determine if brackets are balanced

def balanced(p_string):
    s = Stack()        # declare stack object
    isBalanced = true  # assume that it is balanced
    index = 0          # start at index 0

  #  while index < len(p_string) and isBalanced:
